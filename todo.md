# Updated integration tasks

All requested integration updates are complete. The scraper now uses the preferred direct `/start-scraper` endpoint, the API client keeps the credential isolated behind environment configuration, documentation lists both scraper options, and TypeScript plus production build validation pass.
